autogenerated=0 # remove this line to get rid of the warning on top.
# This is an example Hyprland config file.
# Syntax is the same as in Hypr, but settings might differ.
#
# Refer to the wiki for more information.

exec-once=$HOME/.config/hypr/autostart

monitor=,1920x1080@60,0x0,1
workspace=DP-1,1

input {
    kb_layout = us,es
    kb_options = grp:alt_space_toggle
    kb_options=
    kb_rules=

    follow_mouse=1
}

general {
    sensitivity=1.0 # for mouse cursor
    #main_mod=SUPER

    gaps_in=5
    gaps_out=20
    border_size=2
    col.active_border=0x66fbf1c7
    col.inactive_border=0x66333333

    apply_sens_to_raw=0 # whether to apply the sensitivity to raw input (e.g. used by games where you aim using your mouse)

    #damage_tracking=full # leave it on full unless you hate your GPU and want to make it suffer
}

decoration {
    rounding=6
    blur=0
    blur_size=3 # minimum 1
    blur_passes=1 # minimum 1, more passes = more resource intensive.
    # Your blur "amount" is blur_size * blur_passes, but high blur_size (over around 5-ish) will produce artifacts.
    # if you want heavy blur, you need to up the blur_passes.
    # the more passes, the more you can up the blur_size without noticing artifacts.
    shadow_range=50
    shadow_render_power=1
    col.shadow=0x22000000 # 0xbbf5c2e7
}

animations {
    enabled=1
    animation=windows,1,7,default
    animation=workspaces,1,6,default
}

dwindle {
    pseudotile=0 # enable pseudotiling on dwindle
}

# example window rules
# for windows named/classed as abc and xyz
#windowrule=move 69 420,abc
#windowrule=size 420 69,abc
#windowrule=tile,xyz
#windowrule=float,abc
#windowrule=pseudo,abc
#windowrule=monitor 0,xyz

# example binds
bind=SUPER,Return,exec,alacritty
bind=SUPER,W,killactive,
bind=SUPER,Q,exit,
bind=SUPER,T,exec,pcmanfm
bind=SUPER,V,togglefloating,
bind=SUPER,R,exec,wofi --show drun -o DP-3
bind=SUPER,P,exec,librewolf
bind=SUPER,S,exec,steam
bind=SUPER,D,exec,discord
bind=SUPER,E,exec,prime-run st
bind = SUPER,F10,pass,^(com\.obsproject\.Studio)$ #Record in OBS with that


bind=SUPER,l,movefocus,l
bind=SUPER,h,movefocus,r
bind=SUPER,k,movefocus,u
bind=SUPER,j,movefocus,d

bind=SUPER,1,workspace,1
bind=SUPER,2,workspace,2
bind=SUPER,3,workspace,3
bind=SUPER,4,workspace,4
bind=SUPER,5,workspace,5
bind=SUPER,6,workspace,6
bind=SUPER,7,workspace,7
bind=SUPER,8,workspace,8
bind=SUPER,9,workspace,9
bind=SUPER,0,workspace,10

bind = SUPERSHIFT , 1 , movetoworkspacesilent , 1
bind = SUPERSHIFT , 2 , movetoworkspacesilent , 1
bind = SUPERSHIFT , 3 , movetoworkspacesilent , 1
bind = SUPERSHIFT , 4 , movetoworkspacesilent , 1
bind = SUPERSHIFT , 5 , movetoworkspacesilent , 1
bind = SUPERSHIFT , 6 , movetoworkspacesilent , 1
bind = SUPERSHIFT , 7 , movetoworkspacesilent , 1
bind = SUPERSHIFT , 8 , movetoworkspacesilent , 1
bind = SUPERSHIFT , 9 , movetoworkspacesilent , 1
bind = SUPERSHIFT , 0 , movetoworkspacesilent , 1

bind=SUPERSHIFT,1,movetoworkspace,1
bind=SUPERSHIFT,2,movetoworkspace,2
bind=SUPERSHIFT,3,movetoworkspace,3
bind=SUPERSHIFT,4,movetoworkspace,4
bind=SUPERSHIFT,5,movetoworkspace,5
bind=SUPERSHIFT,6,movetoworkspace,6
bind=SUPERSHIFT,7,movetoworkspace,7
bind=SUPERSHIFT,8,movetoworkspace,8
bind=SUPERSHIFT,9,movetoworkspace,9
bind=SUPERSHIFT,0,movetoworkspace,10

# Mouse binds
bindm=SUPER,mouse:272,movewindow
